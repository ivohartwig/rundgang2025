---
import type { CollectionEntry } from 'astro:content';
export interface Props {
	authors: CollectionEntry<'authors'>[];
}
const { authors } = Astro.props;
---

<section id="hero" class="bg-black text-white">
	<div class="hero-content m-auto max-w-screen-sm p-4">
		<h1>Rundgang 2025</h1>
		<p>Projects from the seminar</p>
		<h2>Fundamentals of Digital Communication</h2>
		<p>in winter semester 2024/2025 and summer semester 2025</p>
		<p class="authors flex flex-row flex-wrap gap-4">
			{
				authors.map((author) => (
					<div class="flex flex-col items-center">
						<a
							href={`https://github.com/${author.data.github_username}`}
							target="_blank"
							rel="noopener noreferrer"
						>
							<img
								src={`https://github.com/${author.data.github_username}.png`}
								alt={author.data.github_username}
								class="rounded-lg object-cover w-16 h-16 sm:w-20 sm:h-20 lg:w-24 lg:h-24 shadow-md hover:scale-105 transition-transform"
							/>
						</a>
						<span class="mt-2 text-center text-base break-all">
							@{author.data.github_username}
						</span>
					</div>
				))
			}
		</p>
	</div>
</section>
