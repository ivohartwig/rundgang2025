---
description: 
globs: *.ts,*.js,*.mjs,*.cjs
alwaysApply: false
---
# Nodejs 

We are using node >= 23. See the .node-version for it. We have access to fetch. No need for node-fetch or whatever.
If possible we use built in moduless.

We also can see the output by running scripts using `node --inspect <file.ts>`

